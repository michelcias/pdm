% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_binomial_localtrend.R
\name{is.binomial_localtrend}
\alias{is.binomial_localtrend}
\title{Check if object is of class binomial_localtrend}
\usage{
is.binomial_localtrend(x)
}
\arguments{
\item{x}{An object to test.}
}
\value{
Logical value: \code{TRUE} if \code{x} inherits from
\code{binomial_localtrend}, \code{FALSE} otherwise.
}
\description{
Test whether an object is of class \code{binomial_localtrend}.
}
\examples{
\donttest{
## Simulate data (same setup as ?mcmc_binomial_localtrend)
set.seed(123)
n <- 500
n_trials <- 20

# Generate true probabilities
grid_vals <- seq_len(n) / n
alpha_true <- (sin(2 * pi * grid_vals) + sin(4 * pi * grid_vals) + 2) / 4

# Generate binomial observations
y <- rbinom(n, size = n_trials, prob = alpha_true)

out <- mcmc_binomial_localtrend(
  y,
  n_trials                = n_trials,
  burnin                  = 1000,
  thinning                = 50,
  n_chain                 = 1000,
  prior_theta01_mean      = 0,
  prior_theta01_prec      = 1,
  prior_theta02_mean      = 0,
  prior_theta02_prec      = 1,
  prior_prec1_shape       = 100,
  prior_prec1_rate        = 1,
  prior_prec2_shape       = 400,
  prior_prec2_rate        = 1,
  lag_update              = 50,
  max_step_size           = 0.1,
  base_adaptation_rate    = 1,
  decay_exponent          = 0.6,
  target_acceptance       = 0.44,
  min_deviation_threshold = NULL,
  return_log_sigma        = FALSE,
  return_accept_prop      = TRUE,
  verbose                 = TRUE,
  bar_width               = 60,
  seed                    = 456
)

is.binomial_localtrend(out)  # TRUE
is.binomial_localtrend(list())  # FALSE
}

}
