% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_poisson_mixture_localtrend.R
\name{is.poisson_mixture_localtrend}
\alias{is.poisson_mixture_localtrend}
\title{Check if object is of class poisson_mixture_localtrend}
\usage{
is.poisson_mixture_localtrend(x)
}
\arguments{
\item{x}{An object to test.}
}
\value{
Logical value: \code{TRUE} if \code{x} inherits from
\code{poisson_mixture_localtrend}, \code{FALSE} otherwise.
}
\description{
Test whether an object is of class \code{poisson_mixture_localtrend}.
}
\examples{
## A minimal fit is all this test needs; see
## ?mcmc_poisson_mixture_localtrend for a realistic analysis.
set.seed(123)
n <- 40
z <- rbinom(n, 1, plogis(cumsum(rnorm(n, sd = 0.2))))
y <- rpois(n, ifelse(z == 1, 8, 1))

out <- mcmc_poisson_mixture_localtrend(
  y,
  link     = "logit",
  burnin   = 50,
  thinning = 1,
  n_draws  = 50,
  verbose  = FALSE,
  seed     = 456
)

is.poisson_mixture_localtrend(out)     # TRUE
is.poisson_mixture_localtrend(list())  # FALSE

}
