% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_probit_bernoulli_localtrend.R
\name{is.probit_bernoulli_localtrend}
\alias{is.probit_bernoulli_localtrend}
\title{Check if object is of class probit_bernoulli_localtrend}
\usage{
is.probit_bernoulli_localtrend(x)
}
\arguments{
\item{x}{An object to test.}
}
\value{
Logical value: \code{TRUE} if \code{x} inherits from
\code{probit_bernoulli_localtrend}, \code{FALSE} otherwise.
}
\description{
Test whether an object is of class \code{probit_bernoulli_localtrend}.
}
\examples{
## A minimal fit is all this test needs; see
## ?mcmc_probit_bernoulli_localtrend for a realistic analysis.
set.seed(123)
n <- 40
y <- rbinom(n, 1, pnorm(cumsum(rnorm(n, sd = 0.2))))

out <- mcmc_probit_bernoulli_localtrend(
  y,
  burnin             = 50,
  thinning           = 1,
  n_draws            = 50,
  prior_theta01_mean = 0,
  prior_theta01_prec = 1,
  prior_theta02_mean = 0,
  prior_theta02_prec = 1,
  prior_prec1_shape  = 1e-2,
  prior_prec1_rate   = 1e-2,
  prior_prec2_shape  = 1e-2,
  prior_prec2_rate   = 1e-2,
  verbose            = FALSE,
  seed               = 456
)

is.probit_bernoulli_localtrend(out)     # TRUE
is.probit_bernoulli_localtrend(list())  # FALSE

}
