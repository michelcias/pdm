% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_locallevel.R
\name{mcmc_locallevel}
\alias{mcmc_locallevel}
\title{Gibbs Sampler for a Local‐Level Dynamic Model}
\usage{
mcmc_locallevel(
  y,
  burnin,
  thinning,
  n_chain,
  prior_theta01_mean,
  prior_theta01_prec,
  prior_prec1_shape,
  prior_prec1_rate,
  prior_prec_y_shape,
  prior_prec_y_rate
)
}
\arguments{
\item{y}{Numeric vector of observations (length = n).}

\item{burnin}{Integer ≥ 0, number of burn‐in iterations.}

\item{thinning}{Integer ≥ 1, thinning interval.}

\item{n_chain}{Integer ≥ 1, number of posterior samples to retain.}

\item{prior_theta01_mean}{Numeric, prior mean for the initial state θ₀₁.}

\item{prior_theta01_prec}{Numeric, prior precision (1/variance) for θ₀₁.}

\item{prior_prec1_shape}{Numeric, shape parameter of the Gamma prior for the innovation precision (1/W₁).}

\item{prior_prec1_rate}{Numeric, rate  parameter of the Gamma prior for the innovation precision (1/W₁).}

\item{prior_prec_y_shape}{Numeric, shape parameter of the Gamma prior for the data precision (1/V).}

\item{prior_prec_y_rate}{Numeric, rate  parameter of the Gamma prior for the data precision (1/V).}
}
\value{
A \code{list} with components:
\describe{
  \item{\code{theta_1}}{Numeric matrix [\code{n_chain} × n] of latent‐state samples.}
  \item{\code{theta_01}}{Numeric vector [length = \code{n_chain}] of initial‐state samples.}
  \item{\code{prec_1}}{Numeric vector [length = \code{n_chain}] of innovation precisions (1/W₁).}
  \item{\code{prec_y}}{Numeric vector [length = \code{n_chain}] of data precisions (1/V).}
}
}
\description{
Runs a Gibbs sampler for the ocal‐level dynamic model
with one state dimension (p = 1).  Burn‐in and thinning are applied
so that exactly \code{n_chain} posterior samples are returned.
}
\examples{
## simulate data
set.seed(123)
n <- 100

# true parameters
theta01_true <- 0
prec1_true   <- 5   # 1/W1
prec_y_true  <- 10  # 1/V

# generate evolution and observation noise
u <- rnorm(n, 0, sqrt(1/prec1_true))
e <- rnorm(n, 0, sqrt(1/prec_y_true))

# latent state and observations
theta1_true <- numeric(n)
theta1_true[1] <- theta01_true + u[1]
for (i in 2:n) {
  theta1_true[i] <- theta1_true[i - 1] + u[i]
}
y <- theta1_true + e

## run the Gibbs sampler
out <- mcmc_locallevel(
  y,
  burnin               = 200,
  thinning             = 10,
  n_chain              = 500,
  prior_theta01_mean   = 0,
  prior_theta01_prec   = 0.01,
  prior_prec1_shape    = 1,
  prior_prec1_rate     = 1,
  prior_prec_y_shape   = 1,
  prior_prec_y_rate    = 1
)

## summarized results
print(summary(out$theta_01))
matplot(t(out$theta_1)[1:5, ], type = "l",
        lty = 1, col = rainbow(5),
        xlab = "time", ylab = "latent state")

}
\seealso{
\link{.Call}
}
