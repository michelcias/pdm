% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_poisson_localacceleration.R
\name{mcmc_poisson_localacceleration}
\alias{mcmc_poisson_localacceleration}
\title{Gibbs Sampler for a Local-Acceleration Poisson Dynamic Model}
\usage{
mcmc_poisson_localacceleration(
  y,
  burnin,
  thinning,
  n_chain,
  prior_theta01_mean,
  prior_theta01_prec,
  prior_theta02_mean,
  prior_theta02_prec,
  prior_theta03_mean,
  prior_theta03_prec,
  prior_prec1_shape = NULL,
  prior_prec1_rate = NULL,
  prior_prec2_shape = NULL,
  prior_prec2_rate = NULL,
  prior_prec3_shape = NULL,
  prior_prec3_rate = NULL,
  lag_update = 50,
  max_step_size = 0.1,
  base_adaptation_rate = 1,
  decay_exponent = 0.6,
  target_acceptance = 0.44,
  min_deviation_threshold = NULL,
  return_log_sigma = FALSE,
  return_accept_prop = FALSE,
  verbose = TRUE,
  bar_width = 60,
  seed = NULL,
  prior_prec1_type = c("halfcauchy", "gamma", "halft"),
  prior_prec1_scale = 2,
  prior_prec1_df = 1,
  prior_prec2_type = c("halfcauchy", "gamma", "halft"),
  prior_prec2_scale = 2,
  prior_prec2_df = 1,
  prior_prec3_type = c("halfcauchy", "gamma", "halft"),
  prior_prec3_scale = 2,
  prior_prec3_df = 1,
  chains = 1,
  parallel = FALSE
)
}
\arguments{
\item{y}{Numeric vector of observed Poisson counts (length \eqn{n}). Each
element must be a non-negative integer.}

\item{burnin}{Integer \eqn{\geq 0}, number of burn-in iterations.}

\item{thinning}{Integer \eqn{\geq 1}, thinning interval.}

\item{n_chain}{Integer \eqn{\geq 1}, number of posterior samples to retain.}

\item{prior_theta01_mean}{Numeric, prior mean for the initial state \eqn{\theta_{0,1}}.}

\item{prior_theta01_prec}{Numeric > 0, prior precision (inverse variance) for \eqn{\theta_{0,1}}.}

\item{prior_theta02_mean}{Numeric, prior mean for the initial state \eqn{\theta_{0,2}}.}

\item{prior_theta02_prec}{Numeric > 0, prior precision (inverse variance) for \eqn{\theta_{0,2}}.}

\item{prior_theta03_mean}{Numeric, prior mean for the initial state \eqn{\theta_{0,3}}.}

\item{prior_theta03_prec}{Numeric > 0, prior precision (inverse variance) for \eqn{\theta_{0,3}}.}

\item{prior_prec1_shape}{Numeric > 0, shape parameter of the Gamma prior for \eqn{1/W_1}. Required (and used) only when \code{prior_prec1_type = "gamma"}.}

\item{prior_prec1_rate}{Numeric > 0, rate parameter of the Gamma prior for \eqn{1/W_1}. Required (and used) only when \code{prior_prec1_type = "gamma"}.}

\item{prior_prec2_shape}{Numeric > 0, shape parameter of the Gamma prior for \eqn{1/W_2}. Required (and used) only when \code{prior_prec2_type = "gamma"}.}

\item{prior_prec2_rate}{Numeric > 0, rate parameter of the Gamma prior for \eqn{1/W_2}. Required (and used) only when \code{prior_prec2_type = "gamma"}.}

\item{prior_prec3_shape}{Numeric > 0, shape parameter of the Gamma prior for \eqn{1/W_3}. Required (and used) only when \code{prior_prec3_type = "gamma"}.}

\item{prior_prec3_rate}{Numeric > 0, rate parameter of the Gamma prior for \eqn{1/W_3}. Required (and used) only when \code{prior_prec3_type = "gamma"}.}

\item{lag_update}{Integer \eqn{\geq 1}, adaptation frequency (sliding window size) for
computing acceptance proportions. Default is 50.}

\item{max_step_size}{Numeric > 0, maximum allowed change in log-scale proposal variance
per adaptation step. Default is 0.1.}

\item{base_adaptation_rate}{Numeric > 0, base rate controlling adaptation speed.
Default is 1.0.}

\item{decay_exponent}{Numeric in (0.5, 1], exponent controlling diminishing adaptation
rate. Default is 0.6.}

\item{target_acceptance}{Numeric in (0,1), target acceptance proportion for
Metropolis-Hastings proposals. Default is 0.44 (theoretically optimal).}

\item{min_deviation_threshold}{Numeric \eqn{\geq 0} or \code{NULL}, minimum absolute deviation
from \code{target_acceptance} required to trigger adaptation. If \code{NULL} (default),
uses practical threshold of \code{1.0/lag_update}. Set to \code{0.0} for maximum sensitivity
(adapt for any deviation). Larger values make adaptation more conservative.}

\item{return_log_sigma}{Logical, whether to return proposal scale diagnostics
(log-scale proposal standard deviations). Default is \code{FALSE}.}

\item{return_accept_prop}{Logical, whether to return acceptance proportion diagnostics
over iterations. Default is \code{FALSE}.}

\item{verbose}{Logical, whether to display a progress bar during sampling. Default is \code{TRUE}.}

\item{bar_width}{Integer in [10, 120], width of the progress bar when \code{verbose = TRUE}.
Default is \code{60}.}

\item{seed}{Optional integer used to set the random number generator seed for
reproducibility. Default is \code{NULL} (no seed set).
When \code{chains > 1} it acts as a master seed from which each chain's own
seed is drawn.}

\item{prior_prec1_type}{Character, prior on the level innovation
precision \eqn{1/W_1}: \code{"halfcauchy"} (default) or \code{"halft"} for a Half-t /
Half-Cauchy prior on \eqn{\sqrt{W_1}} (Gelman, 2006), or \code{"gamma"} for a
Gamma prior on the precision. The default changed in 0.5-0; a call that
supplies \code{prior_prec1_shape}/\code{prior_prec1_rate} without naming a type is
still read as \code{"gamma"}, so existing code keeps working.}

\item{prior_prec1_scale}{Numeric > 0, scale \eqn{A} of the Half-t prior for
\eqn{\sqrt{W_1}}. Default \code{2}, on the scale of the link -- a fixed value
is enough here, unlike the Gaussian family, because the link scale carries
no arbitrary units. Ignored when \code{prior_prec1_type = "gamma"}.}

\item{prior_prec1_df}{Numeric > 0, degrees of freedom \eqn{\nu_1} of the Half-t
prior for \eqn{\sqrt{W_1}}. Default \code{1} (Half-Cauchy).}

\item{prior_prec2_type}{Character, prior on the trend innovation
precision \eqn{1/W_2}: \code{"halfcauchy"} (default) or \code{"halft"} for a Half-t /
Half-Cauchy prior on \eqn{\sqrt{W_2}} (Gelman, 2006), or \code{"gamma"} for a
Gamma prior on the precision. The default changed in 0.5-0; a call that
supplies \code{prior_prec2_shape}/\code{prior_prec2_rate} without naming a type is
still read as \code{"gamma"}, so existing code keeps working.}

\item{prior_prec2_scale}{Numeric > 0, scale \eqn{A} of the Half-t prior for
\eqn{\sqrt{W_2}}. Default \code{2}, on the scale of the link -- a fixed value
is enough here, unlike the Gaussian family, because the link scale carries
no arbitrary units. Ignored when \code{prior_prec2_type = "gamma"}.}

\item{prior_prec2_df}{Numeric > 0, degrees of freedom \eqn{\nu_2} of the Half-t
prior for \eqn{\sqrt{W_2}}. Default \code{1} (Half-Cauchy).}

\item{prior_prec3_type}{Character, prior on the acceleration innovation
precision \eqn{1/W_3}: \code{"halfcauchy"} (default) or \code{"halft"} for a Half-t /
Half-Cauchy prior on \eqn{\sqrt{W_3}} (Gelman, 2006), or \code{"gamma"} for a
Gamma prior on the precision. The default changed in 0.5-0; a call that
supplies \code{prior_prec3_shape}/\code{prior_prec3_rate} without naming a type is
still read as \code{"gamma"}, so existing code keeps working.}

\item{prior_prec3_scale}{Numeric > 0, scale \eqn{A} of the Half-t prior for
\eqn{\sqrt{W_3}}. Default \code{2}, on the scale of the link -- a fixed value
is enough here, unlike the Gaussian family, because the link scale carries
no arbitrary units. Ignored when \code{prior_prec3_type = "gamma"}.}

\item{prior_prec3_df}{Numeric > 0, degrees of freedom \eqn{\nu_3} of the Half-t
prior for \eqn{\sqrt{W_3}}. Default \code{1} (Half-Cauchy).}

\item{chains}{Integer \eqn{\geq 1}, number of independent chains to run.
Default is \code{1}, which returns a single fitted object exactly as before.
With \code{chains > 1} the sampler is re-run once per chain -- each starting
from its own values drawn from the priors -- and an object of class
\code{"pdm_mcmc_list"} is returned, which \code{\link{mcmc_convergence}} turns
into R-hat and effective sample sizes. The progress bar is disabled in this
case, since several bars sharing one console interleave.}

\item{parallel}{Logical, whether to run the chains through
\code{parallel::mclapply()}. Used only when \code{chains > 1}. The result does not
depend on this setting: every chain receives an explicit seed, so a given
\code{seed} reproduces the same output sequentially or in parallel. Default is
\code{FALSE}.}
}
\value{
An object of class \code{c("poisson_localacceleration", "pdm_mcmc", "list")}
with components:
\describe{
\item{\code{theta_1}}{Numeric matrix \eqn{[n_{chain} \times n]} of posterior samples for \eqn{\theta_{t,1}}.}
\item{\code{theta_2}}{Numeric matrix \eqn{[n_{chain} \times n]} of posterior samples for \eqn{\theta_{t,2}}.}
\item{\code{theta_3}}{Numeric matrix \eqn{[n_{chain} \times n]} of posterior samples for \eqn{\theta_{t,3}}.}
\item{\code{theta_01}}{Numeric vector of length \code{n_chain} of posterior samples for \eqn{\theta_{0,1}}.}
\item{\code{theta_02}}{Numeric vector of length \code{n_chain} of posterior samples for \eqn{\theta_{0,2}}.}
\item{\code{theta_03}}{Numeric vector of length \code{n_chain} of posterior samples for \eqn{\theta_{0,3}}.}
\item{\code{prec_theta1}}{Numeric vector of length \code{n_chain} of posterior samples for \eqn{1/W_1}.}
\item{\code{prec_theta2}}{Numeric vector of length \code{n_chain} of posterior samples for \eqn{1/W_2}.}
\item{\code{prec_theta3}}{Numeric vector of length \code{n_chain} of posterior samples for \eqn{1/W_3}.}
\item{\code{alpha}}{Numeric matrix \eqn{[n_{chain} \times n]} of posterior samples for \eqn{\alpha_t} (rates).}
\item{\code{log_sigma}}{(Optional) Numeric matrix \eqn{[n_{chain} \times n]} of proposal scale diagnostics
(if \code{return_log_sigma = TRUE}).}
\item{\code{accept_prop}}{(Optional) Numeric matrix \eqn{[n_{chain} \times n]} of acceptance proportion
diagnostics (if \code{return_accept_prop = TRUE}).}
}

When \code{chains > 1} the return value is instead an object of class
\code{c("pdm_mcmc_list", "list")} holding one such fit per chain; see
\code{\link{print.pdm_mcmc_list}}.
}
\description{
Runs a Gibbs sampler for the local-acceleration Poisson dynamic model
with log link.
}
\details{
The model is defined as:
\deqn{
\begin{aligned}
y_t &\sim \text{Poisson}(\alpha_t), \\
\alpha_t &= \exp(\theta_{t,1}), \\
\theta_{t,1} &= \theta_{t-1,1} + \theta_{t-1,2} + u_{t,1}, & u_{t,1} \sim N(0, W_1), \\
\theta_{t,2} &= \theta_{t-1,2} + \theta_{t-1,3} + u_{t,2}, & u_{t,2} \sim N(0, W_2), \\
\theta_{t,3} &= \theta_{t-1,3} + u_{t,3},                  & u_{t,3} \sim N(0, W_3),
\end{aligned}
}
where \eqn{t = 1, 2, \ldots, n} and \eqn{n} is the number of observations.

The log link function is defined as
\eqn{\exp(x)}, ensuring that \eqn{\alpha_t > 0} as required for Poisson rates.

\strong{Prior Distributions: }

The following conjugate and semi-conjugate prior distributions are employed:

\emph{Initial States:}
\deqn{
\begin{aligned}
\theta_{0,1} &\sim N(\mu_{01}, \tau_{01}^{-1}), \\
\theta_{0,2} &\sim N(\mu_{02}, \tau_{02}^{-1}), \\
\theta_{0,3} &\sim N(\mu_{03}, \tau_{03}^{-1}).
\end{aligned}
}

\emph{Precision Parameters:}

Each innovation precision (\eqn{W_1^{-1}}, \eqn{W_2^{-1}}, \eqn{W_3^{-1}}) may
take one of two priors, chosen independently through \code{prior_prec1_type},
\code{prior_prec2_type} and \code{prior_prec3_type}:

\emph{(a) Gamma prior on the precision} (default, semi-conjugate):
\deqn{W_1^{-1} \sim \text{Gamma}(\nu_1, \eta_1), \quad
      W_2^{-1} \sim \text{Gamma}(\nu_2, \eta_2), \quad
      W_3^{-1} \sim \text{Gamma}(\nu_3, \eta_3).}

\emph{(b) Half-t prior on the standard deviation} (Gelman, 2006):
\deqn{\sqrt{W_1} \sim \text{Half-}t(\nu_1, A_1), \quad
      \sqrt{W_2} \sim \text{Half-}t(\nu_2, A_2), \quad
      \sqrt{W_3} \sim \text{Half-}t(\nu_3, A_3),}
where \eqn{A > 0} is a scale hyperparameter and \eqn{\nu > 0} the degrees of
freedom. Setting \eqn{\nu = 1} gives the Half-Cauchy prior (also selectable
directly with \code{type = "halfcauchy"}); larger \eqn{\nu} approaches a
Half-Normal. The Half-t prior is represented by the inverse-gamma scale
mixture of Wand et al. (2011), which keeps each innovation-precision Gibbs
update closed-form via a single auxiliary variable. This Poisson model has no
Gaussian observation variance, so there is no observation precision \eqn{1/V}.

The Gamma shape/rate arguments are required only when the corresponding
\verb{_type} is \code{"gamma"}; the Half-t scale/df arguments only when it is \code{"halft"}
or \code{"halfcauchy"}. The prior choice is resolved once, before the sampler runs,
and never re-evaluated inside the MCMC loop.

The prior hyperparameters correspond to function arguments as follows:
\tabular{cc}{
\strong{Hyperparameter} \tab \strong{Function Argument} \cr
\eqn{\mu_{01}} \tab \code{prior_theta01_mean} \cr
\eqn{\tau_{01}} \tab \code{prior_theta01_prec} \cr
\eqn{\mu_{02}} \tab \code{prior_theta02_mean} \cr
\eqn{\tau_{02}} \tab \code{prior_theta02_prec} \cr
\eqn{\mu_{03}} \tab \code{prior_theta03_mean} \cr
\eqn{\tau_{03}} \tab \code{prior_theta03_prec} \cr
\eqn{\nu_1} (shape), \eqn{A_1} (scale) \tab \code{prior_prec1_shape}, \code{prior_prec1_scale} \cr
\eqn{\eta_1} (rate), \eqn{\nu_1} (df) \tab \code{prior_prec1_rate}, \code{prior_prec1_df} \cr
\eqn{\nu_2} (shape), \eqn{A_2} (scale) \tab \code{prior_prec2_shape}, \code{prior_prec2_scale} \cr
\eqn{\eta_2} (rate), \eqn{\nu_2} (df) \tab \code{prior_prec2_rate}, \code{prior_prec2_df} \cr
\eqn{\nu_3} (shape), \eqn{A_3} (scale) \tab \code{prior_prec3_shape}, \code{prior_prec3_scale} \cr
\eqn{\eta_3} (rate), \eqn{\nu_3} (df) \tab \code{prior_prec3_rate}, \code{prior_prec3_df}
}

\strong{Adaptive Metropolis-Hastings Algorithm:}

Due to the non-linear observation model with log link, the algorithm
employs component-wise Metropolis-Hastings for sampling the latent states
\eqn{\theta_{t,1}}, with adaptive proposal tuning based on acceptance proportions.
Innovation precisions are sampled from conjugate Gamma posteriors.

The adaptive algorithm uses a diminishing adaptation schedule that ensures
theoretical convergence guarantees (Roberts and Rosenthal, 2007). Adaptation
occurs every \code{lag_update} iterations (e.g., at MCMC iterations 50, 100, 150, ...
if \code{lag_update = 50}), and only after sufficient history has been accumulated
(iteration >= \code{lag_update}).

At each adaptation point (MCMC iteration \eqn{m}), for each time point \eqn{t},
the proposal log-scale standard deviation is updated according to:

\deqn{\log(\sigma_t) \leftarrow \log(\sigma_t) + \text{sign}(\hat{p}_t - p^*) \cdot \gamma_m \cdot \mathbb{1}_{\{|\hat{p}_t - p^*| > \tau\}},}

where:
\itemize{
\item \eqn{\gamma_m} is the diminishing step size at MCMC iteration \eqn{m}, computed as
\deqn{\gamma_m = \min\left(\text{max\_step\_size}, \frac{\text{base\_adaptation\_rate}}{m^\xi}\right);}
\item \eqn{\xi} is the decay exponent (\code{decay_exponent}) applied to the MCMC iteration number;
\item \eqn{\hat{p}_t} is the empirical acceptance proportion at time point \eqn{t}
over the last \code{lag_update} MCMC iterations;
\item \eqn{p^*} is the target acceptance rate (\code{target_acceptance});
\item \eqn{\tau} is the minimum deviation threshold (\code{min_deviation_threshold});
\item \eqn{\mathbb{1}_{\{|\hat{p}_t - p^*| > \tau\}}} is an indicator function that equals 1 when
the condition is true, 0 otherwise.
}

The update only occurs if the absolute deviation exceeds the threshold:
\deqn{|\hat{p}_t - p^*| > \tau.}

This threshold-based approach prevents spurious updates due to random fluctuations.

\strong{Parameter Interactions:}

The adaptive tuning parameters interact as follows:
\itemize{
\item \strong{lag_update}:  Controls both the sliding window size for computing acceptance
proportions AND the frequency of adaptation. Adaptation occurs at MCMC iterations
\eqn{m = k \cdot \text{lag\_update}} for \eqn{k = 1, 2, 3, \ldots}. Larger values
provide more stable estimates but slower adaptation. Common choices:  50-200 iterations.
\item \strong{target_acceptance}:  Optimal acceptance rate for the Metropolis-Hastings
algorithm. The value 0.44 is theoretically optimal for univariate random-walk proposals
(Roberts and Rosenthal, 2001). Each time point \eqn{t} has its own acceptance rate
\eqn{\hat{p}_t}.
\item \strong{min_deviation_threshold}:  Minimum deviation \eqn{|\hat{p}_t - p^*|}
required to trigger adaptation for time point \eqn{t}. The default \code{NULL} uses the
practical threshold \eqn{1/\text{lag\_update}}, corresponding to one additional
acceptance/rejection in the sliding window.
\item \strong{max_step_size}: Maximum allowed change in log-scale proposal variance
per adaptation step. Prevents extreme adjustments. Common choices: 0.01-0.1.
\item \strong{base_adaptation_rate}: Controls the overall speed of adaptation before
decay is applied. Higher values lead to faster but potentially less stable adaptation.
Common choices: 0.1-10.0.
\item \strong{decay_exponent}: Controls how quickly the adaptation step size diminishes
over MCMC iterations. As the algorithm runs, \eqn{\gamma_m} decreases according to
\eqn{m^{-\xi}}. Must be in (0.5, 1] for theoretical convergence guarantees.
Common choices: 0.5-0.8.
}

\strong{Example of Adaptation Schedule:}

With \code{lag_update = 50}, \code{base_adaptation_rate = 1.0}, \code{decay_exponent = 0.6},
and \code{max_step_size = 0.1}:
\itemize{
\item At MCMC iteration 50: \eqn{\gamma_{50} = \min(0.1, 1.0/50^{0.6}) \approx 0.0875}.
\item At MCMC iteration 100: \eqn{\gamma_{100} = \min(0.1, 1.0/100^{0.6}) \approx 0.0631}.
\item At MCMC iteration 1000: \eqn{\gamma_{1000} = \min(0.1, 1.0/1000^{0.6}) \approx 0.0158}.
}

This ensures that adaptation becomes increasingly conservative as the chain progresses,
satisfying theoretical requirements for ergodicity.

\strong{Recommended Settings:}

For most applications:
\itemize{
\item \code{lag_update = 50}: Provides good balance between stability and responsiveness;
\item \code{max_step_size = 0.1}: Conservative adjustment rate;
\item \code{base_adaptation_rate = 1.0}: Moderate adaptation speed;
\item \code{decay_exponent = 0.6}: Standard diminishing adaptation;
\item \code{target_acceptance = 0.44}: Theoretically optimal for univariate proposals;
\item \code{min_deviation_threshold = NULL}: Uses practical default of 1/lag_update.
}

Burn-in and thinning are applied so that exactly \code{n_chain} posterior samples
are returned.
}
\examples{
## Description
# This example demonstrates how to:
# 1. Simulate data from a local-acceleration Poisson dynamic model
# 2. Use `mcmc_poisson_localacceleration` to estimate parameters and latent states
# 3. Perform a detailed posterior analysis with visualizations
# 4. Set a seed for reproducibility

## Simulation of data
set.seed(123)
n <- 200        # Number of observations to simulate

# Generate true rates with acceleration (ensuring positive values)
grid_vals <- seq_len(n) / n
alpha_true <- exp(sin(4 * pi * grid_vals) + sin(8 * pi * grid_vals) + 1)

# Generate Poisson observations
y <- rpois(n, lambda = alpha_true)

## Running the Gibbs sampler
# Run the Gibbs sampler with specified priors and a seed
out <- mcmc_poisson_localacceleration(
  y,
  burnin                  = 1000,
  thinning                = 20,
  n_chain                 = 500,
  prior_theta01_mean      = 0,
  prior_theta01_prec      = 1,
  prior_theta02_mean      = 0,
  prior_theta02_prec      = 1,
  prior_theta03_mean      = 0,
  prior_theta03_prec      = 1,
  prior_prec1_shape       = 100,
  prior_prec1_rate        = 1,
  prior_prec2_shape       = 400,
  prior_prec2_rate        = 1,
  prior_prec3_shape       = 1600,
  prior_prec3_rate        = 1,
  lag_update              = 50,
  max_step_size           = 0.1,
  base_adaptation_rate    = 1,
  decay_exponent          = 0.6,
  target_acceptance       = 0.44,
  min_deviation_threshold = NULL,  # Uses practical default:  1.0/50 = 0.02
  return_log_sigma        = FALSE,
  return_accept_prop      = TRUE,
  verbose                 = TRUE,  # Enable progress bar
  bar_width               = 60,    # Progress bar width
  seed                    = 456
)

## Alternative: weakly-informative Half-Cauchy priors (Gelman, 2006)
# Put a Half-Cauchy prior on the three innovation SDs sqrt(W[1]), sqrt(W[2]),
# sqrt(W[3]); each precision's prior is chosen independently via its `*_type`.
out_hc <- mcmc_poisson_localacceleration(
  y,
  burnin             = 1000,
  thinning           = 20,
  n_chain            = 500,
  prior_theta01_mean = 0,
  prior_theta01_prec = 1,
  prior_theta02_mean = 0,
  prior_theta02_prec = 1,
  prior_theta03_mean = 0,
  prior_theta03_prec = 1,
  prior_prec1_type   = "halfcauchy",
  prior_prec1_scale  = 1,  # sqrt(W[1])
  prior_prec2_type   = "halfcauchy",
  prior_prec2_scale  = 1,  # sqrt(W[2])
  prior_prec3_type   = "halfcauchy",
  prior_prec3_scale  = 1,  # sqrt(W[3])
  verbose            = FALSE,
  seed               = 456
)

## Posterior analysis and visualization
# Use the plot method for comprehensive diagnostics
\donttest{
  # --- Gamma-prior fit (`out`) ---
  # Complete dashboard with all diagnostics
  plot(out, type = "all")

  # Individual diagnostic types
  plot(out, type = "mcmc", which = 1:3)    # Parameter diagnostics
  plot(out, type = "states")               # Dynamic states
  plot(out, type = "alpha")                # Poisson rates
  plot(out, type = "acceptance")           # Acceptance rates

  # Plot with true alpha for comparison
  plot(out, type = "alpha", true_values = list(alpha = alpha_true))

  # --- Half-Cauchy-prior fit (`out_hc`) ---
  # The same diagnostics for the weakly-informative Half-Cauchy fit.
  # (`out_hc` was run with return_accept_prop = FALSE, so the acceptance
  # diagnostic is unavailable for it and is omitted below.)
  plot(out_hc, type = "all")

  plot(out_hc, type = "mcmc", which = 1:3)  # Parameter diagnostics
  plot(out_hc, type = "states")             # Dynamic states
  plot(out_hc, type = "alpha")              # Poisson rates

  # Plot with true alpha for comparison
  plot(out_hc, type = "alpha", true_values = list(alpha = alpha_true))
}

}
\references{
Gelman, A. (2006). Prior distributions for variance parameters in
hierarchical models. \emph{Bayesian Analysis}, 1(3), 515-534.

Roberts, G. O., & Rosenthal, J. S. (2001). Optimal scaling for various
Metropolis-Hastings algorithms. \emph{Statistical Science}, 16(4), 351-367.

Roberts, G. O., & Rosenthal, J. S. (2007). Coupling and ergodicity of adaptive MCMC.
\emph{Journal of Applied Probability}, 44(2), 458-475.

Roberts, G. O., & Rosenthal, J. S. (2009). Examples of adaptive MCMC.
\emph{Journal of Computational and Graphical Statistics}, 18(2), 349-367.
\doi{10.1198/jcgs.2009.06134}

Wand, M. P., Ormerod, J. T., Padoan, S. A., & Fruhwirth, R. (2011). Mean field
variational Bayes for elaborate distributions. \emph{Bayesian Analysis},
6(4), 847-900.

The object also carries \code{pdm_version}, \code{seed} and \code{priors} as attributes:
the version that produced it, the seed as supplied, and every prior after
resolution -- including the ones derived from the data, which never appear in
the call. Splicing \code{attr(fit, "priors")} back into a fresh call reproduces the
fit on any later version, whatever the defaults have become.
}
\seealso{
\code{\link{plot.poisson_localacceleration}},
\code{\link{print.poisson_localacceleration}},
\code{\link{summary.poisson_localacceleration}} for methods on the fitted object;
\code{\link{mcmc_poisson_locallevel}} and
\code{\link{mcmc_poisson_localtrend}} for the other dynamic orders.
}
