% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_probit_bernoulli_localacceleration.R
\name{mcmc_probit_bernoulli_localacceleration}
\alias{mcmc_probit_bernoulli_localacceleration}
\title{Gibbs Sampler for a Local-Acceleration Bernoulli Dynamic Model with Probit Link}
\usage{
mcmc_probit_bernoulli_localacceleration(
  y,
  burnin,
  thinning,
  n_draws,
  prior_theta01_mean,
  prior_theta01_prec,
  prior_theta02_mean,
  prior_theta02_prec,
  prior_theta03_mean,
  prior_theta03_prec,
  prior_prec1_shape = NULL,
  prior_prec1_rate = NULL,
  prior_prec2_shape = NULL,
  prior_prec2_rate = NULL,
  prior_prec3_shape = NULL,
  prior_prec3_rate = NULL,
  verbose = FALSE,
  bar_width = 60,
  seed = NULL,
  prior_prec1_type = c("halfcauchy", "gamma", "halft"),
  prior_prec1_scale = 2,
  prior_prec1_df = 1,
  prior_prec2_type = c("halfcauchy", "gamma", "halft"),
  prior_prec2_scale = 2,
  prior_prec2_df = 1,
  prior_prec3_type = c("halfcauchy", "gamma", "halft"),
  prior_prec3_scale = 2,
  prior_prec3_df = 1,
  chains = 1,
  parallel = FALSE,
  init = NULL
)
}
\arguments{
\item{y}{Numeric vector of observed Bernoulli outcomes (length \eqn{n}). Each
element must be either 0 or 1.}

\item{burnin}{Integer \eqn{\geq 0}, number of burn-in iterations.}

\item{thinning}{Integer \eqn{\geq 1}, thinning interval.}

\item{n_draws}{Integer \eqn{\geq 1}, number of posterior samples to retain.}

\item{prior_theta01_mean}{Numeric, prior mean for the initial state \eqn{\theta_{0,1}}.}

\item{prior_theta01_prec}{Numeric > 0, prior precision (inverse variance) for \eqn{\theta_{0,1}}.}

\item{prior_theta02_mean}{Numeric, prior mean for the initial state \eqn{\theta_{0,2}}.}

\item{prior_theta02_prec}{Numeric > 0, prior precision (inverse variance) for \eqn{\theta_{0,2}}.}

\item{prior_theta03_mean}{Numeric, prior mean for the initial state \eqn{\theta_{0,3}}.}

\item{prior_theta03_prec}{Numeric > 0, prior precision (inverse variance) for \eqn{\theta_{0,3}}.}

\item{prior_prec1_shape}{Numeric > 0, shape parameter of the Gamma prior for \eqn{1/W_1}. Required (and used) only when \code{prior_prec1_type = "gamma"}.}

\item{prior_prec1_rate}{Numeric > 0, rate parameter of the Gamma prior for \eqn{1/W_1}. Required (and used) only when \code{prior_prec1_type = "gamma"}.}

\item{prior_prec2_shape}{Numeric > 0, shape parameter of the Gamma prior for \eqn{1/W_2}. Required (and used) only when \code{prior_prec2_type = "gamma"}.}

\item{prior_prec2_rate}{Numeric > 0, rate parameter of the Gamma prior for \eqn{1/W_2}. Required (and used) only when \code{prior_prec2_type = "gamma"}.}

\item{prior_prec3_shape}{Numeric > 0, shape parameter of the Gamma prior for \eqn{1/W_3}. Required (and used) only when \code{prior_prec3_type = "gamma"}.}

\item{prior_prec3_rate}{Numeric > 0, rate parameter of the Gamma prior for \eqn{1/W_3}. Required (and used) only when \code{prior_prec3_type = "gamma"}.}

\item{verbose}{Logical, whether to display a progress bar during sampling. Default is \code{FALSE}.}

\item{bar_width}{Integer in [10, 120], width of the progress bar when \code{verbose = TRUE}. Default is \code{60}.}

\item{seed}{Optional integer used to set the random number generator seed.
Default is \code{NULL}, which does not set the seed.
When \code{chains > 1} it acts as a master seed from which each chain's own
seed is drawn.}

\item{prior_prec1_type}{Character, prior on the level innovation
precision \eqn{1/W_1}: \code{"halfcauchy"} (default) or \code{"halft"} for a Half-t /
Half-Cauchy prior on \eqn{\sqrt{W_1}} (Gelman, 2006), or \code{"gamma"} for a
Gamma prior on the precision. The default changed in 0.5-0; a call that
supplies \code{prior_prec1_shape}/\code{prior_prec1_rate} without naming a type is
still read as \code{"gamma"}, so existing code keeps working.}

\item{prior_prec1_scale}{Numeric > 0, scale \eqn{A} of the Half-t prior for
\eqn{\sqrt{W_1}}. Default \code{2}, on the scale of the link -- a fixed value
is enough here, unlike the Gaussian family, because the link scale carries
no arbitrary units. Ignored when \code{prior_prec1_type = "gamma"}.}

\item{prior_prec1_df}{Numeric > 0, degrees of freedom \eqn{\nu_1} of the Half-t
prior for \eqn{\sqrt{W_1}}. Default \code{1} (Half-Cauchy).}

\item{prior_prec2_type}{Character, prior on the trend innovation
precision \eqn{1/W_2}: \code{"halfcauchy"} (default) or \code{"halft"} for a Half-t /
Half-Cauchy prior on \eqn{\sqrt{W_2}} (Gelman, 2006), or \code{"gamma"} for a
Gamma prior on the precision. The default changed in 0.5-0; a call that
supplies \code{prior_prec2_shape}/\code{prior_prec2_rate} without naming a type is
still read as \code{"gamma"}, so existing code keeps working.}

\item{prior_prec2_scale}{Numeric > 0, scale \eqn{A} of the Half-t prior for
\eqn{\sqrt{W_2}}. Default \code{2}, on the scale of the link -- a fixed value
is enough here, unlike the Gaussian family, because the link scale carries
no arbitrary units. Ignored when \code{prior_prec2_type = "gamma"}.}

\item{prior_prec2_df}{Numeric > 0, degrees of freedom \eqn{\nu_2} of the Half-t
prior for \eqn{\sqrt{W_2}}. Default \code{1} (Half-Cauchy).}

\item{prior_prec3_type}{Character, prior on the acceleration innovation
precision \eqn{1/W_3}: \code{"halfcauchy"} (default) or \code{"halft"} for a Half-t /
Half-Cauchy prior on \eqn{\sqrt{W_3}} (Gelman, 2006), or \code{"gamma"} for a
Gamma prior on the precision. The default changed in 0.5-0; a call that
supplies \code{prior_prec3_shape}/\code{prior_prec3_rate} without naming a type is
still read as \code{"gamma"}, so existing code keeps working.}

\item{prior_prec3_scale}{Numeric > 0, scale \eqn{A} of the Half-t prior for
\eqn{\sqrt{W_3}}. Default \code{2}, on the scale of the link -- a fixed value
is enough here, unlike the Gaussian family, because the link scale carries
no arbitrary units. Ignored when \code{prior_prec3_type = "gamma"}.}

\item{prior_prec3_df}{Numeric > 0, degrees of freedom \eqn{\nu_3} of the Half-t
prior for \eqn{\sqrt{W_3}}. Default \code{1} (Half-Cauchy).}

\item{chains}{Integer \eqn{\geq 1}, number of independent chains to run.
Default is \code{1}, which returns a single fitted object exactly as before.
With \code{chains > 1} the sampler is re-run once per chain -- each starting
from its own values drawn from the priors -- and an object of class
\code{"pdm_mcmc_list"} is returned, which \code{\link{mcmc_convergence}} turns
into R-hat and effective sample sizes. The progress bar is disabled in this
case, since several bars sharing one console interleave.}

\item{parallel}{Logical, whether to run the chains through
\code{parallel::mclapply()}. Used only when \code{chains > 1}. The result does not
depend on this setting: every chain receives an explicit seed, so a given
\code{seed} reproduces the same output sequentially or in parallel. Default is
\code{FALSE}.}

\item{init}{Optional named list of starting values, or \code{NULL} (the default)
to draw every one of them from its prior, as the sampler has always done.
The names this model accepts are \code{theta_01}, \code{theta_02}, \code{theta_03}, \code{prec_theta1}, \code{prec_theta2}
and \code{prec_theta3};
any subset may be given, and precisions must be positive. An unrecognised
name is an error rather than being ignored, so a misspelling cannot pass
for a starting value that quietly had no effect. With \code{chains > 1} pass a
list of \code{chains} such lists, one per chain: a single starting point shared
by every chain removes the dispersion the Gelman-Rubin statistic in
\code{\link{mcmc_convergence}} is computed from, so it is refused.}
}
\value{
A list with components:
\describe{
\item{\code{theta_1}}{Numeric matrix \eqn{[n_{draws} \times n]} of posterior
samples for the latent level state \eqn{\theta_{t,1}}.}
\item{\code{theta_2}}{Numeric matrix \eqn{[n_{draws} \times n]} of posterior
samples for the latent trend state \eqn{\theta_{t,2}}.}
\item{\code{theta_3}}{Numeric matrix \eqn{[n_{draws} \times n]} of posterior
samples for the latent acceleration state \eqn{\theta_{t,3}}.}
\item{\code{theta_01}}{Numeric vector of length \code{n_draws} of posterior samples
for the initial level state \eqn{\theta_{0,1}}.}
\item{\code{theta_02}}{Numeric vector of length \code{n_draws} of posterior samples
for the initial trend state \eqn{\theta_{0,2}}.}
\item{\code{theta_03}}{Numeric vector of length \code{n_draws} of posterior samples
for the initial acceleration state \eqn{\theta_{0,3}}.}
\item{\code{prec_theta1}}{Numeric vector of length \code{n_draws} of posterior samples
for the level innovation precision \eqn{1/W_1}.}
\item{\code{prec_theta2}}{Numeric vector of length \code{n_draws} of posterior samples
for the trend innovation precision \eqn{1/W_2}.}
\item{\code{prec_theta3}}{Numeric vector of length \code{n_draws} of posterior samples
for the acceleration innovation precision \eqn{1/W_3}.}
\item{\code{alpha}}{Numeric matrix \eqn{[n_{draws} \times n]} of posterior
samples for the Bernoulli probabilities \eqn{\alpha_t}.}
}

When \code{chains > 1} the return value is instead an object of class
\code{c("pdm_mcmc_list", "list")} holding one such fit per chain; see
\code{\link{print.pdm_mcmc_list}}.

The object also carries \code{pdm_version}, \code{seed} and \code{priors} as attributes:
the version that produced it, the seed as supplied, and every prior after
resolution -- including the ones derived from the data, which never appear in
the call. Splicing \code{attr(fit, "priors")} back into a fresh call reproduces the
fit on any later version, whatever the defaults have become.
}
\description{
Runs a Gibbs sampler for the local-acceleration Bernoulli dynamic model
with probit link using Albert-Chib data augmentation.
}
\details{
The model is defined as:
\deqn{
\begin{aligned}
y_t &\sim \text{Bernoulli}(\alpha_t), \\
\alpha_t &= \Phi(\theta_{t,1}), \\
\theta_{t,1} &= \theta_{t-1,1} + \theta_{t-1,2} + u_{t,1}, & u_{t,1} \sim N(0, W_1), \\
\theta_{t,2} &= \theta_{t-1,2} + \theta_{t-1,3} + u_{t,2}, & u_{t,2} \sim N(0, W_2), \\
\theta_{t,3} &= \theta_{t-1,3} + u_{t,3},                  & u_{t,3} \sim N(0, W_3),
\end{aligned}
}
where \eqn{t = 1, 2, \ldots, n}, \eqn{n} is the number of observations, and
\eqn{\Phi(\cdot)} denotes the standard normal cumulative distribution function.

The probit link function is defined as \eqn{\alpha_t = \Phi(\theta_{t,1})}.

\strong{Prior Distributions:}

The following conjugate prior distributions are employed:

\emph{Initial States:}
\deqn{
\begin{aligned}
\theta_{0,1} &\sim N(\mu_{01}, \tau_{01}^{-1}), \\
\theta_{0,2} &\sim N(\mu_{02}, \tau_{02}^{-1}), \\
\theta_{0,3} &\sim N(\mu_{03}, \tau_{03}^{-1}).
\end{aligned}
}

\emph{Precision Parameters:}

Each innovation precision (\eqn{W_1^{-1}}, \eqn{W_2^{-1}}, \eqn{W_3^{-1}}) may
take one of two priors, chosen independently through \code{prior_prec1_type},
\code{prior_prec2_type} and \code{prior_prec3_type}:

\emph{(a) Gamma prior on the precision} (default, semi-conjugate):
\deqn{W_1^{-1} \sim \text{Gamma}(\nu_1, \eta_1), \quad
      W_2^{-1} \sim \text{Gamma}(\nu_2, \eta_2), \quad
      W_3^{-1} \sim \text{Gamma}(\nu_3, \eta_3).}

\emph{(b) Half-t prior on the standard deviation} (Gelman, 2006):
\deqn{\sqrt{W_1} \sim \text{Half-}t(\nu_1, A_1), \quad
      \sqrt{W_2} \sim \text{Half-}t(\nu_2, A_2), \quad
      \sqrt{W_3} \sim \text{Half-}t(\nu_3, A_3),}
where \eqn{A > 0} is a scale hyperparameter and \eqn{\nu > 0} the degrees of
freedom. Setting \eqn{\nu = 1} gives the Half-Cauchy prior (also selectable
directly with \code{type = "halfcauchy"}); larger \eqn{\nu} approaches a
Half-Normal. The Half-t prior is represented by the inverse-gamma scale
mixture of Wand et al. (2011), which keeps each innovation-precision Gibbs
update closed-form via a single auxiliary variable. This probit-Bernoulli
model has no Gaussian observation variance, so there is no observation
precision \eqn{1/V}.

The Gamma shape/rate arguments are required only when the corresponding
\verb{_type} is \code{"gamma"}; the Half-t scale/df arguments only when it is \code{"halft"}
or \code{"halfcauchy"}. The prior choice is resolved once, before the sampler runs,
and never re-evaluated inside the MCMC loop.

The prior hyperparameters correspond to function arguments as follows:
\tabular{cc}{
\strong{Hyperparameter} \tab \strong{Function Argument} \cr
\eqn{\mu_{01}} \tab \code{prior_theta01_mean} \cr
\eqn{\tau_{01}} \tab \code{prior_theta01_prec} \cr
\eqn{\mu_{02}} \tab \code{prior_theta02_mean} \cr
\eqn{\tau_{02}} \tab \code{prior_theta02_prec} \cr
\eqn{\mu_{03}} \tab \code{prior_theta03_mean} \cr
\eqn{\tau_{03}} \tab \code{prior_theta03_prec} \cr
\eqn{\nu_1} (shape), \eqn{A_1} (scale) \tab \code{prior_prec1_shape}, \code{prior_prec1_scale} \cr
\eqn{\eta_1} (rate), \eqn{\nu_1} (df) \tab \code{prior_prec1_rate}, \code{prior_prec1_df} \cr
\eqn{\nu_2} (shape), \eqn{A_2} (scale) \tab \code{prior_prec2_shape}, \code{prior_prec2_scale} \cr
\eqn{\eta_2} (rate), \eqn{\nu_2} (df) \tab \code{prior_prec2_rate}, \code{prior_prec2_df} \cr
\eqn{\nu_3} (shape), \eqn{A_3} (scale) \tab \code{prior_prec3_shape}, \code{prior_prec3_scale} \cr
\eqn{\eta_3} (rate), \eqn{\nu_3} (df) \tab \code{prior_prec3_rate}, \code{prior_prec3_df}
}

The algorithm employs the Albert-Chib (1993) latent variable augmentation
scheme to obtain fully conjugate Gibbs updates for all parameters. This approach
introduces auxiliary Gaussian latent variables whose signs correspond to the
observed binary outcomes, yielding closed-form conditional distributions for
the latent states \eqn{\theta_{t,1}}, \eqn{\theta_{t,2}}, and \eqn{\theta_{t,3}},
the initial states \eqn{\theta_{0,1}}, \eqn{\theta_{0,2}}, and \eqn{\theta_{0,3}},
and the innovation precisions \eqn{1/W_1}, \eqn{1/W_2}, and \eqn{1/W_3}.

Since this is a pure Gibbs sampler with no Metropolis-Hastings steps, the
acceptance rate is always 1.0, eliminating the need for proposal tuning
parameters.

\strong{Progress Bar:}
When \code{verbose = TRUE}, a visual progress bar is displayed showing
\itemize{
\item{Progress bar with adaptive update frequency (based on \code{bar_width})}
\item{Elapsed time in HH:MM:SS format}
\item{Estimated remaining time in HH:MM:SS format}
}

The progress bar update frequency is automatically calculated as approximately
one update per bar segment, ensuring smooth visual feedback with minimal
performance overhead (~0.01\% for typical runs).

Burn-in and thinning are applied so that exactly \code{n_draws} posterior
samples are returned.

\strong{Starting values:}

By default the chain starts from a draw from each parameter's own prior. That
is what makes several chains disperse, and it is what \code{chains > 1} relies on,
but it can also start the chain a long way from the bulk of the posterior --
a Half-Cauchy draw for a standard deviation is occasionally enormous -- and
burn-in then pays for it. \code{init} pins any subset of the parameters to values
of your choosing; everything left out is still drawn from its prior.

The names are the fitted object's own components, which is also the
vocabulary the \code{plot()} method's \code{true_values} argument uses, so one name
means one parameter across the package. \code{init} covers the scalar parameters
only: the latent trajectories, and the \code{alpha} probabilities derived from
them, cannot be set -- each trajectory starts flat at its own initial state.
}
\examples{
## Description
# This example demonstrates how to:
# 1. Simulate data from a multi-frequency sinusoidal probability pattern
# 2. Use `mcmc_probit_bernoulli_localacceleration` to estimate parameters and latent states
# 3. Perform a detailed posterior analysis with visualizations
# 4. Set a seed for reproducibility
# 5. Use progress bar for monitoring MCMC execution

## Simulation of data
n <- 200  # Number of observations to simulate

# Use a fixed seed for data simulation
set.seed(123)

# Generate true success probabilities following a multi-frequency sinusoidal pattern
grid_vals <- seq_len(n) / n
alpha_true <- (sin(4 * pi * grid_vals) + sin(8 * pi * grid_vals) + 2) / 4

# Generate Bernoulli observations
y <- rbinom(n, size = 1, prob = alpha_true)

## Running the Gibbs sampler with progress bar
# NOTE: these MCMC controls are sized to keep the example quick, and are
# not guaranteed to be enough for convergence -- how much burn-in and
# thinning a fit needs depends on the data and on the family. For real
# work, fit with chains > 1 and read mcmc_convergence() before trusting
# any summary.
out <- mcmc_probit_bernoulli_localacceleration(
  y,
  burnin             = 1000,
  thinning           = 20,
  n_draws            = 500,
  prior_theta01_mean = 0,
  prior_theta01_prec = 1,
  prior_theta02_mean = 0,
  prior_theta02_prec = 1,
  prior_theta03_mean = 0,
  prior_theta03_prec = 1,
  prior_prec1_shape  = 100,
  prior_prec1_rate   = 1,
  prior_prec2_shape  = 400,
  prior_prec2_rate   = 1,
  prior_prec3_shape  = 1600,
  prior_prec3_rate   = 1,
  verbose            = TRUE,
  bar_width          = 60,
  seed               = 456
)

## Alternative: weakly-informative Half-Cauchy priors (Gelman, 2006)
# Put a Half-Cauchy prior on the three innovation SDs sqrt(W[1]), sqrt(W[2]),
# sqrt(W[3]); each precision's prior is chosen independently via its `*_type`.
# This fit also pins two starting values through `init`. Note that the
# initial state lives on the scale of the linear predictor -- the probit
# scale, hence qnorm() of the observed proportion, not the proportion
# itself. `prec_theta1` is pinned too, since a Half-Cauchy draw for an
# innovation SD is occasionally enormous and burn-in then pays for the
# journey back.
out_hc <- mcmc_probit_bernoulli_localacceleration(
  y,
  burnin             = 1000,
  thinning           = 20,
  n_draws            = 500,
  prior_theta01_mean = 0,
  prior_theta01_prec = 1,
  prior_theta02_mean = 0,
  prior_theta02_prec = 1,
  prior_theta03_mean = 0,
  prior_theta03_prec = 1,
  prior_prec1_type   = "halfcauchy",
  prior_prec1_scale  = 1,  # sqrt(W[1])
  prior_prec2_type   = "halfcauchy",
  prior_prec2_scale  = 1,  # sqrt(W[2])
  prior_prec3_type   = "halfcauchy",
  prior_prec3_scale  = 1,  # sqrt(W[3])
  init               = list(theta_01    = qnorm(mean(y)),
                            prec_theta1 = 10),
  seed               = 456
)
# Where the chain actually started is recorded on the fit:
attr(out_hc, "init")

## Posterior analysis and visualization
# Use the plot method for comprehensive diagnostics
\donttest{
  # --- Gamma-prior fit (`out`) ---
  # Complete dashboard with all diagnostics
  plot(out, type = "all")

  # Individual diagnostic types
  plot(out, type = "mcmc", which = 1:3)    # Initial states (theta_0)
  plot(out, type = "mcmc", which = 4:6)    # Innovation precisions (W^-1)
  plot(out, type = "states")               # Dynamic states
  plot(out, type = "alpha")                # Bernoulli probabilities

  # Plot with true probabilities for comparison
  plot(out, type = "alpha", true_values = list(alpha = alpha_true))

  # --- Half-Cauchy-prior fit (`out_hc`) ---
  # The same diagnostics for the weakly-informative Half-Cauchy fit.
  plot(out_hc, type = "all")

  plot(out_hc, type = "states")            # Dynamic states

  plot(out_hc, type = "alpha", true_values = list(alpha = alpha_true))
}

}
\references{
Albert, J. H., & Chib, S. (1993). Bayesian analysis of binary and polychotomous
response data. \emph{Journal of the American Statistical Association}, 88(422),
669-679. \doi{10.1080/01621459.1993.10476321}

Gelman, A. (2006). Prior distributions for variance parameters in
hierarchical models. \emph{Bayesian Analysis}, 1(3), 515-534.

Wand, M. P., Ormerod, J. T., Padoan, S. A., & Fruhwirth, R. (2011). Mean field
variational Bayes for elaborate distributions. \emph{Bayesian Analysis},
6(4), 847-900.
}
\seealso{
\code{\link{plot.probit_bernoulli_localacceleration}},
\code{\link{print.probit_bernoulli_localacceleration}},
\code{\link{summary.probit_bernoulli_localacceleration}} for methods on the fitted object;
\code{\link{mcmc_probit_bernoulli_locallevel}} and
\code{\link{mcmc_probit_bernoulli_localtrend}} for the other dynamic orders.
}
