% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdm_mcmc_list_methods.R
\name{pdm_mcmc_list-methods}
\alias{pdm_mcmc_list-methods}
\alias{summary.pdm_mcmc_list}
\alias{print.summary.pdm_mcmc_list}
\alias{log_lik.pdm_mcmc_list}
\alias{waic.pdm_mcmc_list}
\alias{loo.pdm_mcmc_list}
\alias{plot.pdm_mcmc_list}
\title{Methods for multi-chain pdm fits}
\usage{
\method{summary}{pdm_mcmc_list}(object, rhat_threshold = 1.01, ...)

\method{print}{summary.pdm_mcmc_list}(x, digits = 4L, ...)

\method{log_lik}{pdm_mcmc_list}(object, ...)

\method{waic}{pdm_mcmc_list}(x, rhat_threshold = 1.01, ...)

\method{loo}{pdm_mcmc_list}(x, rhat_threshold = 1.01, ...)

\method{plot}{pdm_mcmc_list}(x, type = "mcmc", which = NULL, true_values = NULL, ask = NULL, ...)
}
\arguments{
\item{rhat_threshold}{Numeric > 1, the \eqn{\hat{R}} above which \code{summary()},
\code{waic()} and \code{loo()} warn that the chains have not converged. Default
\code{1.01}, matching \code{\link{mcmc_convergence.pdm_mcmc_list}}.}

\item{...}{Passed to the underlying single-chain method.}

\item{x, object}{An object of class \code{"pdm_mcmc_list"} — except in
\code{print.summary.pdm_mcmc_list()}, where \code{x} is what \code{summary()} returned.}

\item{digits}{Integer, significant digits for the R-hat column. Default 4.}

\item{type}{Character, which diagnostic to draw. Accepts the same values as
the plot method of the underlying model family (see
\code{\link{plot.normal_locallevel}} and its siblings). Default \code{"mcmc"}.}

\item{which}{Optional integer vector selecting scalar parameters for
\code{type = "mcmc"}, indexed as in the single-chain plot methods. \code{NULL} (the
default) draws all of them.}

\item{true_values}{Optional named list of true values to overlay, as in the
single-chain plot methods.}

\item{ask}{Logical, whether to pause between pages. Defaults to
\code{interactive()} when more than one page will be drawn.}
}
\value{
\code{summary()} returns what the single-chain method returns for that
model class, with class \code{"summary.pdm_mcmc_list"} prepended and three
extra elements: \code{chains}, \code{n_chain_each}, \code{rhat} (a named vector of
rank-normalized split-\eqn{\hat{R}}, one per scalar parameter) and
\code{rhat_states} (the same, at twenty time points along each trajectory).
\code{log_lik()} returns a draws-by-observations matrix pooled over chains;
\code{waic()} and \code{loo()} the corresponding \pkg{loo} objects; \code{plot()} returns
\code{x} invisibly.
}
\description{
Methods that let an object returned with \code{chains > 1} be used
wherever a single-chain fit would be. Except for \code{plot(type = "mcmc")},
they work by stacking the chains into one equivalent fit and delegating to
the existing single-chain method, so results are identical to what the same
number of draws from one chain would give.
}
\details{
\subsection{Why pooling is the right default}{
Once the chains have converged they are draws from the same posterior, so
four chains of 1000 estimate posterior quantities exactly as 4000 draws
would — better, in fact, since the draws are less autocorrelated. Every
method here that reports a posterior quantity (\code{summary}, \code{log_lik},
\code{waic}, \code{loo}, and every \code{plot} type other than \code{"mcmc"}) therefore pools.

The exception is anything that reads the draws as a \emph{sequence}. A pooled
trace plot splices chain 2 onto the end of chain 1 and shows a jump that
means nothing, so \code{plot(type = "mcmc")} keeps the chains apart and draws
one series per chain.
}

\subsection{\code{summary()} carries its own convergence evidence}{
Pooling is only meaningful once the chains agree, and a pooled summary
looks exactly the same whether they do or not. \code{summary()} therefore
computes the rank-normalized split-\eqn{\hat{R}} of every scalar parameter,
stores it on the returned object, and prints it underneath the usual
tables. When the largest exceeds \code{rhat_threshold} it also raises a
warning at call time, so the caveat survives being scrolled past or
captured.

Both the scalar parameters and a sample of twenty time points along each
latent trajectory are checked. An earlier version checked only the scalars,
on the stated grounds that they are the slowest-mixing part of these
models. That is true of the Gaussian family and false of the link
families: measured at the settings the examples use, \code{binomial}, \code{poisson}
and \code{probit} all had \emph{states} with higher \eqn{\hat{R}} than any scalar,
and a scalar-only screen therefore reported all-clear on fits whose
trajectories had not converged. For every time point rather than a sample,
use \code{\link{mcmc_convergence}}.
}

\subsection{\code{waic()} and \code{loo()} warn too}{
They pool the chains exactly as \code{summary()} does, and a \code{"loo"} object gives
no hint whether the draws behind it agreed. Both therefore raise the same
warning above \code{rhat_threshold}. Model comparison is where an unconverged
fit does the most damage, since the number it produces looks like every
other elpd.
}

\subsection{\code{loo()} uses the chain structure}{
\code{loo.pdm_mcmc_list()} is not a plain delegation. The single-chain method
has to tell \pkg{loo} that all draws come from one chain
(\code{chain_id = rep(1, ...)}), which makes the relative efficiency used in the
PSIS smoothing a cruder estimate. With several chains the real chain
identifiers are passed instead, so \code{r_eff} — and therefore the Pareto
diagnostics — rest on a better estimate.
}
}
\examples{
\donttest{
set.seed(123)
n <- 150
y <- cumsum(c(10, rnorm(n)))[-1] + rnorm(n, sd = sqrt(1 / 5))

fits <- mcmc_normal_locallevel(
  y,
  burnin             = 1000,
  thinning           = 5,
  n_chain            = 500,
  prior_theta01_mean = y[1],
  prior_theta01_prec = 1 / var(y),
  prior_prec1_shape  = 1e-2,
  prior_prec1_rate   = 1e-2,
  prior_prec_y_shape = 1e-2,
  prior_prec_y_rate  = 1e-2,
  chains             = 4,
  seed               = 456
)

## Always check convergence before reading a pooled summary
mcmc_convergence(fits)

## Per-chain diagnostics: one series per chain, R-hat in the header
plot(fits, type = "mcmc", which = 1)

## Everything else pools the chains
plot(fits, type = "states")
summary(fits)

## Model comparison, with r_eff estimated from the real chains.
## `loo` is a generic of the loo package, so qualify it (or attach loo first).
loo::loo(fits)
loo::waic(fits)
}

}
\seealso{
\code{\link{mcmc_convergence.pdm_mcmc_list}} for the numerical
diagnostics; \code{\link{print.pdm_mcmc_list}}; \code{\link{pdm_compare}}
for ranking several fits.
}
