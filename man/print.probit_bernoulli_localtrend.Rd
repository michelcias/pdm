% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_probit_bernoulli_localtrend.R
\name{print.probit_bernoulli_localtrend}
\alias{print.probit_bernoulli_localtrend}
\title{Print method for probit_bernoulli_localtrend objects}
\usage{
\method{print}{probit_bernoulli_localtrend}(x, digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{probit_bernoulli_localtrend}.}

\item{digits}{Integer, number of decimal places to display. Default is 3.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns the input object \code{x}.
}
\description{
Prints a concise summary showing posterior medians.
Use \code{summary()} for comprehensive statistics when available.
}
\details{
This method provides a quick overview using posterior medians,
which are robust to outliers and skewness in the posterior distribution.
}
\examples{
\donttest{
## Simulate data (same setup as ?mcmc_probit_bernoulli_localtrend)
n <- 200

set.seed(123)
grid_vals <- seq_len(n) / n
alpha_true <- (sin(2 * pi * grid_vals) + sin(4 * pi * grid_vals) + 2) / 4
y <- rbinom(n, size = 1, prob = alpha_true)

out <- mcmc_probit_bernoulli_localtrend(
  y,
  burnin             = 1000,
  thinning           = 20,
  n_draws            = 500,
  prior_theta01_mean = 0,
  prior_theta01_prec = 1,
  prior_theta02_mean = 0,
  prior_theta02_prec = 1,
  prior_prec1_shape  = 100,
  prior_prec1_rate   = 1,
  prior_prec2_shape  = 400,
  prior_prec2_rate   = 1,
  verbose            = TRUE,  # Enable progress bar
  bar_width          = 60,    # Progress bar width
  seed               = 456
)

print(out)
}

}
\seealso{
\code{\link{mcmc_probit_bernoulli_localtrend}} (model generator),
\code{\link{plot.probit_bernoulli_localtrend}}, \code{\link{summary.probit_bernoulli_localtrend}}.
}
