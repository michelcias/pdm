% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_binomial_locallevel.R
\name{print.summary.binomial_locallevel}
\alias{print.summary.binomial_locallevel}
\title{Print method for summary.binomial_locallevel objects}
\usage{
\method{print}{summary.binomial_locallevel}(x, digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{summary.binomial_locallevel}, typically
the result of calling \code{summary()} on a \code{binomial_locallevel}
object.}

\item{digits}{Integer, number of significant digits to display. Default is 3.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns the input object \code{x}.
}
\description{
Prints comprehensive posterior statistics in a readable format.
}
\examples{
\donttest{
## Simulation of data
n <- 500
n_trials <- 20
theta0_true <- 0.5
prec1_true <- 100

set.seed(123)
u1 <- rnorm(n, sd = sqrt(1/prec1_true))

theta1_true <- cumsum(c(theta0_true, u1))[-1]
alpha_true <- plogis(theta1_true)
y <- rbinom(n, size = n_trials, prob = alpha_true)

out <- mcmc_binomial_locallevel(
  y,
  n_trials                = n_trials,
  burnin                  = 1000,
  thinning                = 50,
  n_chain                 = 1000,
  prior_theta01_mean      = 0,
  prior_theta01_prec      = 1,
  prior_prec1_shape       = 100,
  prior_prec1_rate        = 1,
  lag_update              = 50,
  max_step_size           = 0.1,
  base_adaptation_rate    = 1,
  decay_exponent          = 0.6,
  target_acceptance       = 0.44,
  min_deviation_threshold = NULL,
  return_log_sigma        = FALSE,
  return_accept_prop      = TRUE,
  verbose                 = TRUE,
  bar_width               = 60,
  seed                    = 456
)

s <- summary(out)
print(s)
# or simply:
summary(out)
}

}
\seealso{
\code{\link{summary.binomial_locallevel}},
  \code{\link{print.binomial_locallevel}}
}
