% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_poisson_locallevel.R
\name{print.summary.poisson_locallevel}
\alias{print.summary.poisson_locallevel}
\title{Print method for summary.poisson_locallevel objects}
\usage{
\method{print}{summary.poisson_locallevel}(x, digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{summary.poisson_locallevel}, typically
the result of calling \code{summary()} on a \code{poisson_locallevel}
object.}

\item{digits}{Integer, number of significant digits to display. Default is 3.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns the input object \code{x}.
}
\description{
Prints comprehensive posterior statistics in a readable format.
}
\examples{
\dontrun{
## Simulation of data
n <- 500
theta0_true <- 0.5
prec1_true <- 100

set.seed(123)
u1 <- rnorm(n, sd = sqrt(1/prec1_true))

theta1_true <- cumsum(c(theta0_true, u1))[-1]
alpha_true <- exp(theta1_true)
y <- rpois(n, lambda = alpha_true)

out <- mcmc_poisson_locallevel(
  y,
  burnin                  = 1000,
  thinning                = 50,
  n_chain                 = 1000,
  prior_theta01_mean      = 0,
  prior_theta01_prec      = 1,
  prior_prec1_shape       = 100,
  prior_prec1_rate        = 1,
  lag_update              = 50,
  max_step_size           = 0.1,
  base_adaptation_rate    = 1,
  decay_exponent          = 0.6,
  target_acceptance       = 0.44,
  min_deviation_threshold = NULL,
  return_log_sigma        = FALSE,
  return_accept_prop      = TRUE,
  verbose                 = TRUE,
  bar_width               = 60,
  seed                    = 456
)

s <- summary(out)
print(s)
# or simply:
summary(out)
}

}
\seealso{
\code{\link{summary.poisson_locallevel}},
  \code{\link{print.poisson_locallevel}}
}
