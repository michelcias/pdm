% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_probit_bernoulli_locallevel.R
\name{print.summary.probit_bernoulli_locallevel}
\alias{print.summary.probit_bernoulli_locallevel}
\title{Print method for summary.probit_bernoulli_locallevel objects}
\usage{
\method{print}{summary.probit_bernoulli_locallevel}(x, digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{summary.probit_bernoulli_locallevel}, typically
the result of calling \code{summary()} on a \code{probit_bernoulli_locallevel}
object.}

\item{digits}{Integer, number of significant digits to display. Default is 3.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns the input object \code{x}.
}
\description{
Prints comprehensive posterior statistics in a readable format.
}
\examples{
\donttest{
## Simulation of data
n <- 200
set.seed(123)
alpha_true <- (sin(2 * pi * seq_len(n) / n) + 2) / 4
y <- rbinom(n, size = 1, prob = alpha_true)

out <- mcmc_probit_bernoulli_locallevel(
  y,
  burnin             = 1000,
  thinning           = 20,
  n_draws            = 500,
  prior_theta01_mean = 0,
  prior_theta01_prec = 1,
  prior_prec1_shape  = 100,
  prior_prec1_rate   = 1,
  verbose            = TRUE,
  bar_width          = 60,
  seed               = 456
)

s <- summary(out)
print(s)
# or simply:
summary(out)
}

}
\seealso{
\code{\link{summary.probit_bernoulli_locallevel}},
\code{\link{print.probit_bernoulli_locallevel}}
}
