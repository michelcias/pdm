% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_probit_bernoulli_localtrend.R
\name{print.summary.probit_bernoulli_localtrend}
\alias{print.summary.probit_bernoulli_localtrend}
\title{Print method for summary.probit_bernoulli_localtrend objects}
\usage{
\method{print}{summary.probit_bernoulli_localtrend}(x, digits = 3, ...)
}
\arguments{
\item{x}{An object of class \code{summary.probit_bernoulli_localtrend}, typically
the result of calling \code{summary()} on a \code{probit_bernoulli_localtrend}
object.}

\item{digits}{Integer, number of significant digits to display. Default is 3.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns the input object \code{x}.
}
\description{
Prints comprehensive posterior statistics in a readable format.
}
\examples{
\donttest{
## Simulation of data
n <- 500
set.seed(123)
grid_vals <- seq_len(n) / n
alpha_true <- (sin(2 * pi * grid_vals) + sin(4 * pi * grid_vals) + 2) / 4
y <- rbinom(n, size = 1, prob = alpha_true)

out <- mcmc_probit_bernoulli_localtrend(
  y,
  burnin             = 1000,
  thinning           = 50,
  n_chain            = 1000,
  prior_theta01_mean = 0,
  prior_theta01_prec = 1,
  prior_theta02_mean = 0,
  prior_theta02_prec = 1,
  prior_prec1_shape  = 100,
  prior_prec1_rate   = 1,
  prior_prec2_shape  = 400,
  prior_prec2_rate   = 1,
  verbose            = TRUE,
  bar_width          = 60,
  seed               = 456
)

s <- summary(out)
print(s)
# or simply:
summary(out)
}

}
\seealso{
\code{\link{summary.probit_bernoulli_localtrend}},
  \code{\link{print.probit_bernoulli_localtrend}}
}
