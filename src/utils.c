void generate_normal_vector(int n) {
#ifdef DEBUG
  if (n <= 2) {
    error("generate_normal_vector: dimension n must be > 2, got %d", n);
  }
#endif
  /* ========== Matrix Parameters Setup ========== */
  // existing code...
}